app-id: io.github.perfect_dark_pc_port.perfect_dark
runtime: org.freedesktop.Platform
runtime-version: "25.08"
sdk: org.freedesktop.Sdk
command: io.github.perfect_dark_pc_port.perfect_dark.sh

finish-args:
  - --device=all
  - --share=ipc
  - --socket=x11
  # Audio
  - --socket=pulseaudio
  # Enable network for multiplayer
  - --share=network

modules:
  - name: perfect_dark
    buildsystem: simple
    sources:
      - type: file
        path: bin/pd.x86_64
      - type: file
        path: bin/pd.jpn.x86_64
      - type: file
        path: bin/pd.pal.x86_64
      - type: file
        path: io.github.perfect_dark_pc_port.perfect_dark.sh
      - type: file
        path: io.github.perfect_dark_pc_port.perfect_dark.png
      - type: file
        path: io.github.perfect_dark_pc_port.perfect_dark.metainfo.xml
      - type: file
        path: io.github.perfect_dark_pc_port.perfect_dark.desktop
    build-commands: []
    post-install:
      # Install the binaries
      - install -Dm 755 -t /app/bin/ pd.*
      - install -Dm 755 ${FLATPAK_ID}.sh /app/bin/${FLATPAK_ID}.sh
      - install -Dm 644 ${FLATPAK_ID}.desktop /app/share/applications/${FLATPAK_ID}.desktop
      - install -Dm 644 ${FLATPAK_ID}.metainfo.xml /app/share/metainfo/${FLATPAK_ID}.metainfo.xml
      - install -Dm 644 ${FLATPAK_ID}.png /app/share/icons/hicolor/256x256/apps/${FLATPAK_ID}.png
